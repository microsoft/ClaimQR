Sample issuance and verification application for Claim QRs; `src/server.ts` provides a sample server with issuance and verification endpoints.

To launch:

``` 
npm install
npm run build
npm run deploy
```
